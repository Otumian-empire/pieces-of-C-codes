#ifndef MSTRING_H
#define MSTRING_H

// return length of char*
int len(char *str);

// return char at index, else, '\0'
char char_at(char *str, int a);

// return index of char, else, -1
int index_of(char *str, char a);

#endif
